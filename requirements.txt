streamlit>=1.30
numpy>=1.26
pandas>=2.1
scipy>=1.12
matplotlib>=3.8

# TensorFlow is intentionally NOT a dependency. Hybrid mode's BiLSTM
# surrogates run as a pure-NumPy forward pass over the exact trained
# weights stored in the <organ>_surrogate.pkl files (see app.py's
# surrogate-inference section) -- verified against the original
# TensorFlow/Keras model to within float32 rounding before this change.
# This means the app has no dependency that lacks wheels for whichever
# Python version a host (e.g. Streamlit Community Cloud) happens to run.
